# Copy to .env at repo root for Docker. Do not commit .env.
# For manual backend runs, use nba-tracker-api/.env instead (see nba-tracker-api/.env.example).

GROQ_API_KEY=
NBA_API_PROXY=
FRONTEND_URL=
